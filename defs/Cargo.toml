[package]
name = "yaspar-macros-defs"
version = "0.1.1"
edition = "2024"
# Kept in step with the macro crate, the only thing that depends on this one.
rust-version = "1.88"
readme = "README.md"
license = "Apache-2.0"
description = "The fixed definitions that `yaspar-macros` expansions refer to: the step protocol, its driver, the pinned store, and a stable stand-in for `Try`."
authors = ["the Amazon Trusted Solver team"]
keywords = ["macros", "recursion", "stack"]
categories = ["development-tools::procedural-macro-helpers", "rust-patterns"]
homepage = "https://github.com/yaspar-org/yaspar-macros"
repository = "https://github.com/yaspar-org/yaspar-macros"
